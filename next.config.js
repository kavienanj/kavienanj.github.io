/** @type {import('next').NextConfig} */
const nextConfig = {
  /* GitHub Pages Config */
  output: "export",
  // basePath: "/kavienanj.github.io",
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig
